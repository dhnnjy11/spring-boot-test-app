pipeline {
    agent {
        docker {
            image "maven:3.6.3-openjdk-17-slim"
        }
    }


    stages {
        stage('Build') {
            steps {
               sh "mvn --version"
                sh 'mvn -f cacheable-data clean install'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}